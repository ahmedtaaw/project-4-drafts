function doubleIt(val){
    return val*2
}

console.log([1,2,3,4,5].map(doubleIt))

/**
 * example of using map
 * -from lower to upper case
 * -translations: list of strings to their translation into forgien language
 * -list of string and trancating them into a certain length
 * -prefix and sufix
 * -list of functions, array that have a bunch of functions
 * -list of functions and mapping them to their return or recall this function
 */